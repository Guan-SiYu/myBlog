//标记周末
function markWeekend(row,index){
 return (row<20)&&((index+1)%7==0||(index+2)%7==0)? true :false ;
}
function isHoliday(index){
 return index>19 ? true : false;
}
module.exports={
  markWeekend:markWeekend,
  isHoliday:isHoliday
}
